name: Deploy CONECTA Premium Max

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact main commit
        uses: actions/checkout@v7
        with:
          ref: ${{ github.sha }}
      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - name: Install locked dependencies
        run: npm ci --ignore-scripts --no-audit --no-fund
      - name: Structure, tests and type check
        run: |
          set -o pipefail
          npm run check 2>&1 | tee check.log
      - name: Upload check diagnostics
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: conecta-check-log-${{ github.run_id }}
          path: check.log
          if-no-files-found: ignore
          retention-days: 7
      - name: Build
        run: npm run build
      - name: Bundle performance budget
        run: node scripts/check-bundle-budget.mjs
      - name: Install Chromium
        run: npx playwright install --with-deps chromium
      - name: Essential browser gate (smoke + map security)
        timeout-minutes: 3
        run: npx playwright test e2e/smoke.spec.js e2e/map-security.spec.js
      - name: Configure Pages
        uses: actions/configure-pages@v6
      - name: Upload exact build artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy exact validated build to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
